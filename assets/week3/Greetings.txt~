1. comma between instructions
    addi $t0 $t0 4 --> addi $t0, $t0, 4
	beq $t4, $t0 else
2. never branch to main, have another label after main, and branch to that
    b main

3. unnecessary branches:
    b exit

exit:

4. proper indentation

\t\t.data
\t\t.text
main:
    addi[space]$t0,[space]$t0,[space]4
loop:

5. 
